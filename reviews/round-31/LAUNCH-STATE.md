# Round 31 — launched (fresh independent round after the round-30 revision)

Snapshot: `.agent-runtime/review-snapshots/round-31`
SNAPSHOT_ID: `09a606a73d9f475ec624785b7b2a8483f989f2270eca7aeb405015205d113c84`
Files: 606 (`missing []`)
Working-tree HEAD at freeze: `40a49ae` (the round-30 revision commit)
Built artifact: `build/main.pdf`, 34 pages, 0 overfull boxes, 0 undefined references or
citations. Supplement: `build/anisotropic-biot-2026-09-20-v2.zip`, 69 files,
sha256 `9643b64b8e7b4150a45bcb1bdacccbb286f245f24bec2933f6d3f2082c342d16`.

## Why rounds 29 and 30 do not carry forward

Rounds 29 and 30 each returned three MINOR REVISION verdicts and **0 exact ACCEPT**, so
neither met the >=2 criterion. The round-30 revision changed scientific, notation and
provenance content (a corrected justification clause, the general bar rule, the barred-stress
normalizations, the `e_4`/`e_5` basis members, a citation-key correction, and regenerated
run provenance), so no round-29 or round-30 verdict is counted here. Counting only exact
ACCEPT verdicts on the round-31 snapshot; **>=2 required**.

Round 29 also recorded an independence incident (its reviewer 1 read part of another
report); that reviewer's return was discarded and the round-31 reviewers are all fresh.

## What changed since round 30

Eight required corrections, all recorded in `reviews/round-30/response.md`:

1. `sections/pore_fabric.tex` — the §7.4 justification no longer asserts that the whole
   equilibrium potential is independent of the complementary modes; it restricts the claim
   to the distention energy and states the dropped modes are frozen by construction.
2. `fe-evidence/runs/fabric_*/provenance.json` (19 records) — regenerated by re-running the
   decks, so each now records a revision that contains the fabric sources together with
   `input_sha256`, a three-file `source_sha256` map, and `binary_sha256`.
3. `build/weighted-stress/derivation-scan.txt` and `display-scan.txt` — renamed off `.json`
   and with the foreign absolute path rewritten repository-relative.
4. `main.tex` — the bar rule is anchored to the general multiplicative decomposition.
5. `main.tex` — the barred stresses now record their differing normalizations.
6. `main.tex` — a prime / double-prime rule is added to the notation paragraph.
7. `sections/pore_fabric.tex` — `e_4` and `e_5` labelled and normalized with `sqrt2`.
8. `sections/pore_fabric.tex` — the minimization reference retargeted to
   `eq:fabric-equilibrium`.

Also applied: `\usepackage{placeins}` with a `\FloatBarrier` at the end of §9 (Figures 6–11
now render on pp. 25–28 instead of after the References); `braun2020` renamed `braun2021`
(the published year is 2021); `tools/build_review_snapshot.py` now includes the four tracked
paths declared by `provenance/manuscript-export.json`; a note recording the two deliberately
separate copies of the manufactured-solution analysis. Nine optional notes were retained
unapplied, with reasons, in the round-30 response.

## Known facts a reviewer should not misread

- `site/evidence.json` records `source_revision` `b6d7272…`, the HEAD at the moment the site
  manifests were regenerated; the current HEAD is `40a49ae`. The manifest's own note says the
  base commit predates the working revision.
- Every quoted finite-element number is unchanged from the round-30 snapshot. The pore-fabric
  decks were re-run for provenance, and the re-run reproduces the recorded scalar histories
  byte for byte and the Exodus field arrays bit for bit.
- The pore-fabric provenance records now carry `binary_sha256` as well as the older
  `application_sha256`; the two are the same digest of the compiled application.

## Rules for this round

- Reviewers read the immutable snapshot, never the working tree, and never edit the manuscript.
  Each reviewer writes only `reviews/round-31/reviewer-{1,2,3}.md`.
- **Do not open any file under `reviews/`.** Reading another reviewer's report invalidates that
  reviewer's vote; self-report any accidental exposure.
- Each reviewer confirms that `sha256(snapshot/source-manifest.json)` equals the declared
  SNAPSHOT_ID, re-hashes every listed file, and reports mismatches or missing files.
- Stable comment IDs with exact source locations; required changes separated from optional
  notes; exactly one verdict line.
- Held suites (`validation/`, `examples/verify_*.py`) are not run.
- SIMULATED AI PEER REVIEW, not journal acceptance.

## Complementary emphasis

1. Derivation and correctness.
2. Numerical verification and source fidelity.
3. Exposition, notation and claims.
