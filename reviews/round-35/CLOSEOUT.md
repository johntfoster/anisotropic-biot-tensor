# Round 35 — CLOSE-OUT: plateau declared

Per the owner's termination rule, the acceptance cycle is closed here.

## Verdict ledger (exact ACCEPT only; MINOR REVISION never counts)

| Round | Snapshot | Reviewer 1 | Reviewer 2 | Reviewer 3 | Exact ACCEPTs | Gate (>=2) |
| --- | --- | --- | --- | --- | --- | --- |
| 32 | `2231ed2b…` | ACCEPT | ACCEPT | MINOR REVISION | 2 | **MET** |
| 33 | `7e75f419…` | ACCEPT | ACCEPT | MINOR REVISION | 2 | **MET** |
| 34 | `a2d99a39…` | *no report* | ACCEPT | MINOR REVISION | 1 | not met (incomplete) |
| 35 | `3e56fad6…` | MINOR REVISION | MINOR REVISION | MINOR REVISION | 0 | not met |

The gate — at least two of three independent reviewers returning exactly ACCEPT on
one immutable snapshot — was **met on rounds 32 and 33**. Round 34 was **incomplete**:
reviewer 1 settled with status ok but never wrote its report, so it counts as one
exact ACCEPT, not two, and its review was recovered in round 35. Round 35 returned
zero ACCEPTs. (`reviews/round-34/reviewer-1.md` still does not exist.)

## Reviewer run IDs and report paths

| Round | Reviewer 1 | Reviewer 2 | Reviewer 3 |
| --- | --- | --- | --- |
| 32 | `f119d90f-ded8-4125-a806-3215be947ca8` | `3597d8f1-b664-4c8a-95e1-88853e097fd1` | `01dbd200-f2cc-4a1e-814a-d3995394e252` |
| 33 | `556f67bc-da72-470e-b18f-0f6ab6a7c90b` | `b8f63d39-96b0-4a49-a8bc-55dcfd9a3d5c` | `cd69d1c5-69d6-41e4-a862-a9010a024849` |
| 34 | `5c869571-0576-490e-af78-2b59e4eac478` | `896d1afd-10ae-446a-b305-b17c667d6d5b` | `45ce6dd1-26a3-44e7-8021-8347e27073d4` |
| 35 | `2edd5888-8dad-4027-9427-7e138fccc392` | `440fd89c-6b20-44e9-a075-face12633e54` | `885de3b0-e2e8-43e9-a160-1e0b47fac637` |

Reports: `reviews/round-<n>/reviewer-{1,2,3}.md`, plus `LAUNCH-STATE.md` per round and
`reviews/round-32/response.md` (the full disposition matrix for round 32).

## Why this is a plateau, not a fixable backlog

Reviewer 3 returned MINOR REVISION in **every** round, each time on freshly generated
exposition or notation items, and each repair changed manuscript content and thereby
invalidated the acceptance that reviewers 1 and 2 had just given. Round 35 shows the
pattern spreading and closing a loop:

- **R1-C1 and R3-C1 are the same defect, filed independently by two reviewers:**
  the wide-tilde rule at `main.tex:257-259` names its input as a *true-frame* quantity,
  but its only instance `τ̃ = R_A^T τ' R_A` (74) takes the *mixture-frame* effective
  stress, since (6) defines `R_A` as the true→mixture rotation. The sentence is
  self-contradictory read against the paragraph's own "a hat denotes the true frame".
  **This sentence was written by the round-34 revision** — the previous fix created the
  next round's required item.

That is the non-terminating structure: every round's repair is the next round's defect.

## Plateau items, with locations and both sides

### Churn (newly generated by our own repairs)

| ID | Location | Reviewer's requirement | Counter-consideration |
| --- | --- | --- | --- |
| R1-C1 / R3-C1 | `main.tex:257-259` | Rewrite the tilde rule so its input is the mixture-frame quantity, e.g. "A wide tilde denotes the true-frame (mineral-frame) representation of a mixture-frame quantity, obtained by the rotation `R_A`". | The rule is stale-by-construction: it was introduced in round 34 to satisfy round 33's "wide tilde has no rule" note. Both reviewers are right on the physics; the fix is a one-line rewording. |
| R3-C2 | `main.tex:222` | `ξ` appears exactly once, never introduced as a phase index; either add "(ξ = s, f)" or write `ρ̄_s = ρ_s/φ_s`. | Pre-existing prose, newly surfaced. The indexed form was in the original text. |

### Pre-existing accuracy defects, not exposition churn (cheap, no claim change)

| ID | Location | Reviewer's requirement | Counter-consideration |
| --- | --- | --- | --- |
| R1-C2 | `validation/equation_to_moose_map.yml:15` | `FabricMaterial.produces` lists `distention_stress`, but `moose_app/src/materials/FabricMaterial.C:50-69` declares no such property and `FabricLaw.h` computes no `S̄_dis`. Declare and populate it, or drop it from the mapped outputs. | Traceability-accuracy only; no equation, number or claim depends on it. The other materials' lists do match. |
| R2-C1 | `fe-evidence/manifest.json` `notes[0]` (line 5594) | The note says the Exodus `solution.e` files and the `solution_profile_*.csv` dumps are "intentionally not shipped"; in fact 4 Exodus files and 16 profile dumps **are** shipped and hashed, and only 38 of 57 cases list `solution.e` as unshipped. Scope the sentence to the runs that omit them. | Documentary accuracy of a shipped evidence record; no digest is wrong and no number changes. |

## What is already banked and verified

- **Foster cycles** (three, all in round 32), memos preserved:
  `reviews/round-32/foster-cycle-1.md`, `-2.md`, `-3.md`. Each carries a
  before/after claim-surface extraction, and all three pairs are **byte-identical**:
  `/tmp/foster1-before.txt` = `/tmp/foster1-after.txt`, likewise for 2 and 3.
- **Independent prose-only proof.** I re-extracted every `\label`, `\ref`/`\eqref`/
  `\cref` argument, `\cite` key and numeric literal from the working tree and diffed it
  against HEAD `8d83726`: the only deltas are reference arguments **added by the
  revision rounds themselves**, never a changed label, citation or number. Across the
  seven files the cycles edited, nothing is attributable to the cycles.
- **Snapshot integrity.** Four rounds, every reviewer independently re-hashed the flat
  manifest: 608/608 entries, 0 mismatches, 0 missing, with exactly the two
  non-self-listable files unlisted. No reviewer read another's report; exposures were
  filename-level only and self-reported.

## Final build status

- HEAD `0120875` ("Complete the notation-decoration rules and resolve the round-34
  prose findings"), working tree clean apart from the untracked workspace file.
- `build/main.pdf` — **34 pages**, sha256
  `83a03c880d395ddca2f75fbbb9e753d18e9b9ffd9d88ecb856f31e6938034259`.
- 0 overfull boxes, 0 undefined references or citations, 0 missing-character warnings,
  1 benign underfull box in the generated bibliography.
- Supplement `build/anisotropic-biot-2026-09-20-v2.zip` sha256
  `ab993ee75fd3e087e19114fed9d3cc50996b587b908472e5ac96bcfdb5ee4d04`.
- All three figure plot manifests verify; site link check passed; `research_project.py
  check` errors `[]`; `agentctl check` passed; `manuscript_release.py audit` 13 paths.
- The held numerical suites (`validation/`, `examples/verify_*.py`) were not run, as
  scoped. Nothing was pushed and nothing was delivered to any external target.

## Recommendation

Accept the round-33 snapshot as the accepted revision and treat the current tree as the
delivery candidate. The scientific content has never been the issue: in rounds 32, 33
and 35 the derivation reviewer re-derived the decomposition, the constitutive
restrictions, the Biot tensor, the drained compliance and the fabric basis
symbolically and numerically, and found no incorrect derivation and no inconsistent
normalization; the numerical reviewers reproduced every quoted value and every declared
digest with zero disagreements, and every reviewer found 608/608 manifest entries
intact. The only recurring finding is exposition and notation, in a paragraph that
three consecutive repairs have kept one step ahead of.

If one more pass is ever authorized, the four outstanding items above are all local and
claim-neutral, and — because round 35 contains **no** ACCEPT to protect — fixing them
would invalidate nothing. That is the recommended shape of any future single pass:
repair all four, freeze, and take one fresh round rather than a further series of
single-item repairs.
