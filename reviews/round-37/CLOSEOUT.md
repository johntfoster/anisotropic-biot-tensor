# Round 37 — CLOSE-OUT: the gate was not met, and the cycle stops here

## Verdict ledger (exact ACCEPT only; MINOR REVISION never counts)

| Round | Snapshot | Reviewer 1 | Reviewer 2 | Reviewer 3 | Exact ACCEPTs | Gate (>=2) |
| --- | --- | --- | --- | --- | --- | --- |
| 32 | `2231ed2b…` | ACCEPT | ACCEPT | MINOR REVISION | 2 | **MET** |
| 33 | `7e75f419…` | ACCEPT | ACCEPT | MINOR REVISION | 2 | **MET** |
| 34 | `a2d99a39…` | *no report* | ACCEPT | MINOR REVISION | 1 | not met (incomplete) |
| 35 | `3e56fad6…` | MINOR REVISION | MINOR REVISION | MINOR REVISION | 0 | not met |
| 36 | `c1f6210d…` | ACCEPT (recovered seat) | MINOR REVISION | MINOR REVISION | 1 | not met |
| 37 | `0d75448b…` | MINOR REVISION (recovery stub) | MINOR REVISION | MINOR REVISION | **0** | **not met** |

**The current tree did not reach two accepts.** Round 37 returned zero exact ACCEPTs on its one
immutable snapshot. Per the owner's bound this is the last round; no round 38 was opened and no
further repair was made.

## Reviewer run IDs and report paths

| Seat | Run ID | Session key | Report |
| --- | --- | --- | --- |
| Reviewer 1 (first run — **no report written**) | `4c13836c-aca9-44bd-884e-a8c667fdd23e` | `agent:react:dashboard:ae54ecba-4296-482c-b6a1-ab8fd59220b2` | *(none)* |
| Reviewer 1 (recovery run — wrote a **skeleton only**) | `aba97fa1-7590-4872-879e-a477ecd6428b` | `agent:react:dashboard:6dbee912-a8ca-4c2c-aef2-14f9929669e2` | `reviews/round-37/reviewer-1.md` (824 bytes) |
| Reviewer 2 | `935667c5-73fc-4e94-8290-c7d05fc2a5da` | `agent:react:dashboard:88c5d7b3-1faa-4a52-b182-8dafcf3b40d8` | `reviews/round-37/reviewer-2.md` (14 507 bytes) |
| Reviewer 3 | `4df8f030-aaf1-4c8f-84f1-6f57573f600e` | `agent:react:dashboard:afa937be-87b2-4faa-8a42-e0ae8a39c593` | `reviews/round-37/reviewer-3.md` (15 475 bytes) |

Seat 1 failed the same way as in round 36: its first run settled with status ok after writing no
file (its transcript shows real verification — it recomputed `B_0 = [0.7, 0.75833333, 0.79166667]`,
reproduced `C^d` from the drained restriction with zero residual, and matched the deck parameters —
but it never wrote the report). The seat was recovered in a fresh isolated run; the recovery run
wrote the required file, but only the pre-agreed skeleton, with "no required items identified" and
verdict MINOR REVISION. A recovered review is never counted as an acceptance, and with seats 2 and
3 both returning MINOR REVISION the two-accept gate was already unreachable, so no third recovery
run was spent.

## Snapshot integrity

`.agent-runtime/review-snapshots/round-37`, `SNAPSHOT_ID`
`0d75448ba758ff51c9f4e43423c120ffba6eaa1088442206436692c957fd2189`, 608 files. All three seats
independently confirmed `sha256(source-manifest.json)` equals the declared SNAPSHOT_ID and re-hashed
every listed file: 608/608, 0 mismatches, 0 listed-but-missing, 0 present-but-unlisted (excluding the
two self-referential metadata files). Two seats self-reported an accidental directory *name* listing
under `reviews/` (no report content, verdict, or count displayed).

## Round-36 items — disposition and verification

| Item | Location | Disposition |
| --- | --- | --- |
| R2-C1 (dangling evidence id; unregistered report; manifest fails its own validator) | `site/evidence.json`, generator `tools/populate_site_manifest.py` | **Fixed.** The generator now falls back to the already-published `site/reports/<id>.json` when the ignored `build/` copy is absent, and a new guard refuses to emit an id it did not register. Regenerated by running the generator chain (not hand-patched). `--validate-only` → `{"manifest": "valid", "artifacts": 41, ...}`, exit 0; full site build passes; reviewer 2 independently re-confirmed the validator and all 41 artifact digests. |
| R2-O1 (`reference_note` wording) | `fe-evidence/README.md`, generator `tools/materialize_fe_evidence.py` | **Fixed.** Scoped to `reference_comparable:false`; generator literal and shipped file proven byte-identical (1162 bytes) by AST extraction. |
| R2-O2 (companion-manifest digest mismatch) | `references/notes/weighted-stress-source-manifest.json` | **No action, verified.** It records a companion revision (`companion_commit e62234c8…`, keys `paper/main.tex` etc.) that is not this repository's tree. Reviewer 2 reached the same conclusion independently (its R2-O2). |
| R2-O3 (positive verification summary) | — | No action; a positive summary. |
| R3-C1 (hat and wide tilde both "the true frame") | `main.tex` notation paragraph | **Fixed.** The decorations are now partitioned on normalization, not frame: hat = mineral-normalized (`sigma-hat_s` per current mineral volume, `tau-hat_s` per reference mineral volume); wide tilde = mixture-normalized rotated into the true frame. The round-trip case `tau-hat_s = R_A^T tau-bar_s R_A` (11) is stated to keep the hat. Reviewer 3 verified the whole scheme against every occurrence (199 bar, 11 hat, 4 tilde, 23 `^d`, 39 `_dis`, 6 `^n`, 124 subscript-0) and raised no item on the hat/tilde pair. |
| R3-C2 ("fixed-pressure" names a double-primed stress and a single-primed energy) | `main.tex` notation paragraph and pressure-coupling section | **Fixed.** The epithet now follows the mark: single prime carries the full pore-pressure term (`sigma'`, `P'`); double prime is the reduced counterpart (`P''`, `W''`); `W'` is the single-primed companion. Reviewer 3 raised no item on it. |

## Round-37 required items, with both sides

### R2-C1 — a shipped dependency is missing from the shipped requirements file
**Seat:** reviewer 2 (numerical verification and source fidelity).
**Location:** `examples/requirements.txt:1-4` (also bundled in
`build/anisotropic-biot-2026-09-20-v2.zip`); the archive's `README.md` reproduction procedure;
`examples/plot_fabric_contours.py:40`.
**Reviewer's side:** the requirements file declares only `numpy`, `scipy`, `matplotlib`, but
`plot_fabric_contours.py` imports `netCDF4` unconditionally (used at line 73), and the figure plot
manifest records `netCDF4 1.7.4` as the version actually used. The archive README states the figures
are reproducible from the extracted archive by running that script; in a fresh environment the
declared install set yields `ModuleNotFoundError: No module named 'netCDF4'` and the two contour /
diffusion figures cannot be regenerated.
**Counter-consideration:** every digest surface — including the archive's own 68-entry internal
manifest — verifies byte-for-byte, and no quoted number is affected; `netCDF4` is present in the
environment that produced the figures. This is a dependency-completeness gap in a documentation
file, not a numerical or evidence-integrity failure. It was not fixed because the owner's bound
stops the cycle here.

### R3-C1 — `P_0` breaks the stated subscript-0 rule
**Seat:** reviewer 3 (exposition, notation and claims).
**Location:** `sections/finite_elements.tex:188` (`p = P_0 cos(πX₁)cos(πX₂)sin t`) and
`sections/finite_elements.tex:191` (`U = P_0 = 0.01`), against `main.tex:261-262` ("a subscript 0
marks a quantity of the reference configuration").
**Reviewer's side:** of 124 subscript-0 decorations, 122 are reference-configuration quantities and
2 are not: `P_0` is the manufactured-solution pressure **amplitude**, so a reader applying the stated
rule would read it as a reference-state pressure.
**Counter-consideration:** `P_0` is a locally introduced named amplitude constant, the rule's own
exemplars are reference-state quantities, and no equation, number or claim turns on the reading.
Either renaming the amplitude or amending the rule closes it; it is a small local notation fix.

### Reviewer 1 (derivation and correctness) — no required item delivered
The seat's first run wrote nothing; its recovery run wrote only a skeleton with "no required items
identified yet" and verdict MINOR REVISION. Its substantive derivation findings are therefore not
recorded from this round. Its first run's transcript shows it re-derived the decomposition, the
phase balance, the drained restriction and the deck parameters without finding an error.

## Build status

- Working-tree HEAD after the repair commit `3144a68`; `build/main.pdf` — **33 pages**, sha256
  `795826c3ed141007d674e159b7b0056b9a67cf3cf3988ed63d470e873ce832fb`; 0 overfull boxes, 0 undefined
  references or citations, 0 missing-character warnings, 1 benign underfull bibliography box, 106
  numbered equations (unchanged from round 36).
- `tools/check_figure_manifests.py` verifies 3 manifests; `research_project.py check` errors `[]`;
  `agentctl check` passes; the site validator and full site build pass; the supplement archive's
  internal manifest is 68/68 with exact payload coverage. The held numerical suites were not run,
  as scoped.

## Recommendation

None is offered: the bound closes the cycle and forbids round 38. The two outstanding required items
are local and claim-neutral — add `netCDF4` to `examples/requirements.txt` and re-package the
supplement; rename or except the manufactured-solution amplitude `P_0`.
