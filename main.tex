\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,bm,mathtools}
\usepackage{booktabs,tabularx}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepgfplotslibrary{groupplots}
\makeatletter
\let\tablerows\@@input
\makeatother
\usepackage[numbers,sort&compress]{natbib}
\usepackage[colorlinks=true,allcolors=blue]{hyperref}
\usepackage[nameinlink,noabbrev]{cleveref}
\newcommand{\F}{\mathbf{F}}
\newcommand{\Fb}{\bar{\mathbf{F}}}
\newcommand{\A}{\mathbf{A}}
\newcommand{\I}{\mathbf{I}}
\newcommand{\C}{\mathbb C}
\newcommand{\D}{\mathbb D}
\newcommand{\Hh}{\mathbb H}
\newcommand{\Pvol}{\mathbb P_{\mathrm{vol}}}
\newcommand{\Pdev}{\mathbb P_{\mathrm{dev}}}
\newcommand{\tr}{\operatorname{tr}}
\newcommand{\sym}{\operatorname{sym}}
\newcommand{\dd}{\,\mathrm d}
\newcommand{\pder}[2]{\frac{\partial #1}{\partial #2}}
\newcommand{\atp}{\big|_{p}}
\newcommand{\ate}{\big|_{\bm e}}
\providecommand{\doi}[1]{\href{https://dx.doi.org/#1}{#1}}

\title{Mineral-volume compatibility and anisotropic pressure coupling at finite deformation}
\author{John T. Foster}
\date{}
\hypersetup{pdftitle={Mineral-volume compatibility and anisotropic pressure coupling at finite deformation},pdfauthor={John T. Foster}}

\begin{document}
\maketitle

\begin{abstract}
A saturated anisotropic solid can require different stress increments in
its principal directions to prevent deformation during a pore-pressure change.
We express this pressure tangent through the equilibrium response of true
mineral volume in a one-solid/one-fluid mixture. The construction connects a
finite-deformation effective-stress decomposition to an experimentally defined
pressure sensitivity and to the established pore-volume conjugacy. Its
constitutive implications are examined using a constrained skeleton--mineral
energy. Under isotropic distention, the direct mineral contribution to
directional coupling is its volumetric--deviatoric stiffness block; purely
deviatoric mineral anisotropy does not contribute to that block. The classical
solid-storage restriction, expressed in the mineral-volume
coordinate, supplies a compatibility test beyond recovery of the Biot stress
coefficient. This condition exposes the limitation of identifying the
effective mineral-volume tangent with a volume-scaled isolated mineral
modulus. Compatible anisotropic-mineral examples separate direct stiffness effects
from the effects of compliance and coupling calibration. Two objective
hyperelastic continuations provide reproducible pressure and layer tests. In the illustrative finite-strain
regime, freezing the reference Biot tensor underpredicts layer extension by
about 9\% and lateral reaction magnitude by about 11\%. The two energies share their reference tangents and complete drained response,
but predict different pressure curvatures, providing a finite-pressure
identification test. An integrability condition also shows why a spatially
constant anisotropic pressure tensor cannot define a hyperelastic law on an
open set of finite coaxial strains.
\end{abstract}

\noindent\textbf{Keywords:} finite-deformation poromechanics; anisotropic
elasticity; Biot tensor; mineral stiffness; Legendre transform

\section{Introduction}
\label{sec:introduction}

The linear theory of a fluid-saturated anisotropic porous solid was formulated
by Biot \citep{biot1955anisotropic}.  Its pressure--stress coupling requires,
in addition to the fourth-order elastic tensor, a symmetric second-order
effective-stress (Biot--Willis) tensor and a storage coefficient
\citep{biotwillis1957,thompsonwillis1991}.  These coupling coefficients are independent phenomenological data in a
general linear model. Additional micromechanical assumptions can relate them
to the elastic moduli. For example, the microisotropic construction reviewed
and used by Wong \citep[sec.~2.1]{wong2017} relates directional Biot
coefficients to drained stiffnesses and the isotropic constituent modulus.
It is therefore necessary to distinguish independence in a constitutive family
from independence under specified microstructural constraints.

Experiments on Callovo--Oxfordian claystone illustrate why this distinction
matters. Braun et al.\ \citep{braun2020} report strong elastic and Skempton
anisotropy but relatively weak anisotropy in the back-calculated Biot
coefficients. Elastic anisotropy alone is consequently an insufficient guide
to the magnitude of directional pressure coupling. Their use of drained,
pore-pressure, and deviatoric loading also illustrates the measurements needed
to distinguish these responses.

Finite-deformation volume conjugacy is established poromechanical structure.
De Buhan et al.\ \citep{debuhan1998} construct a skeleton potential in the
macroscopic deformation gradient and reference-normalized pore volume, and
analyze the conditions for a pressure-independent Terzaghi effective stress.
Zhao and Borja \citep{zhaoborja2020} develop an anisotropic elastoplastic
formulation with an objective stress rate and a Biot tensor expressed through
the elastic moduli and intrinsic bulk modulus. Their derivation uses a
barotropic elastic solid constituent and a small elastic correction in the
solid mass balance; it is not simply a theory of infinitesimal total
kinematics. In our earlier work \citep{fosterxu2025}, a pressure Legendre
transform identifies a finite-deformation scalar Biot coefficient through the
fixed-pressure sensitivity of intrinsic solid volume to skeleton volume.

Here that scalar sensitivity is extended to the full skeleton deformation.
The resulting tensor is both a state-dependent function and an incremental
pressure tangent; these descriptions are compatible. The general identity
organizes the constitutive analysis but does not prescribe a material law or
replace the established volume-potential formulation. The main constitutive
questions are which mineral and coupling properties control this tangent,
and which extra conditions make its reference limit compatible with a
homogeneous solid under unjacketed loading.

We address these questions using isotropic distention and a constrained
quadratic skeleton--mineral energy. The reduction isolates the direct
volumetric--deviatoric mineral contribution and the mixed mineral-volume
tangent. Requiring both the classical stress response and classical solid storage
reveals a compatibility failure in the restricted daughter-energy ansatz that
a stress-only calibration misses. This is a diagnosis of that ansatz, not a
new independent poroelastic coefficient. Compatible anisotropic-mineral examples connect this diagnosis to the
directional mechanism. Two objective energies then separate finite-pressure
model dependence from the error of freezing a reference pressure tensor. A
finite-strain integrability test explains the limitation of that approximation. The numerical parameters define a reproducible
constitutive example rather than a calibration to a particular rock.

\begin{table}[tb]
\centering\small
\caption{Relationship to the closest formulations. The comparison concerns
constitutive structure, not historical priority of volume conjugacy.}
\label{tab:prior-formulations}
\begin{tabularx}{\textwidth}{@{}p{.23\textwidth}XX@{}}
\toprule
Formulation & Constitutive structure & Relation to the present analysis\\
\midrule
De Buhan et al.\ \citep{debuhan1998} & Finite-strain skeleton potential in
 deformation and pore volume; microscopic boundary-value construction &
 Establishes volume conjugacy and conditions for pressure-independent
 effective stress.\\
Wong \citep{wong2017} & Linear anisotropic response with microisotropic
 constituent and crack fabric & Modulus-based reference coupling under
 additional microscopic assumptions.\\
Zhao--Borja \citep{zhaoborja2020} & Objective elastic stress rate, barotropic
 constituent, anisotropic plasticity & Rate-based coupling, classical solid storage, and engineering
 simulations; distinct from a specified hyperelastic mineral-volume energy.\\
Foster--Xu \citep{fosterxu2025} & Mixture kinematics and pressure transform &
 Scalar precursor recovered when mineral volume depends on skeleton volume.\\
Present work & Objective energy with an equilibrated mineral-volume
 coordinate & Tensor sensitivity, constrained mineral contribution,
 unjacketed compatibility, and finite-pressure examples.\\
\bottomrule
\end{tabularx}
\end{table}

\section{Kinematics and thermodynamic setting}
\label{sec:kinematics}

Let \(\F\) map the solid skeleton reference configuration to its current
configuration, and let \(J=\det\F>0\) be the corresponding skeleton volume
ratio.  The solid partial density is
\(\rho_s=\phi_s\bar\rho_s\), where \(\phi_s\) is the solid volume fraction
and \(\bar\rho_s\) is the intrinsic density of the mineral solid.  For a
uniform reference state, solid mass conservation gives
\begin{equation}
 J\rho_s=\rho_{s0}=\phi_{s0}\bar\rho_{s0}.
 \label{eq:solid-mass}
\end{equation}
The true mineral volume ratio is
\begin{equation}
 \bar J\equiv\frac{\bar\rho_{s0}}{\bar\rho_s},
 \qquad
 \phi_s=\frac{\phi_{s0}\bar J}{J},
 \qquad \phi_f=1-\phi_s.
 \label{eq:distention}
\end{equation}
Thus \(\bar J\) is the mineral volume ratio, \(\phi_f\) is the fluid volume
fraction, and \(J/\bar J\) is the distention ratio of the skeleton.  In other
words, \(J\) measures the change of the porous skeleton volume, whereas
\(\bar J\) measures the change of volume of the mineral itself.  We separate
these two changes using the
distention--true-deformation construction emphasized by Drumheller
\citep{drumheller2000},
\begin{equation}
 \F=\A\Fb,
 \qquad \det\A=\frac{J}{\bar J},
 \qquad \det\Fb=\bar J.
\label{eq:true-mineral-jacobian}
\end{equation}
Here \(\A\) describes distention and \(\Fb\) describes true mineral
deformation.  The determinant relation in
\eqref{eq:true-mineral-jacobian} is enough to track volume.  We use the
isotropic-distention specialization below, which also fixes the shape part of
the true mineral deformation once \(\F\) and \(\bar J\) are known.
For the nonreacting, one-solid/one-fluid specialization considered here, an
isotropic distention \(\A=a\I\), with
\(a=(J/\bar J)^{1/3}\), makes \(\F=a\Fb\) kinematically equivalent, up to
the naming and ordering of factors, to the isotropic swelling--elastic
multiplicative split used in Flory-type network theories \citep{flory1961}.
This is a kinematic equivalence only: it neither transfers Flory's mixing free
energy nor supplies the present pressure-conjugacy or constitutive assumptions.

Let \(q=\ln\bar J\) be the mineral volume coordinate.  At the fixed
temperature considered here, let \(\psi_s(\F,q)\) be the solid specific
Helmholtz free energy and define the mixture-reference free-energy density by
\(W(\F,q)=\rho_{s0}\psi_s(\F,q)\).  The normalization used
in our earlier work \citep{fosterxu2025} takes the intrinsic solid specific
volume as \(\bar v_s=1/\bar\rho_s=\bar v_{s0}\bar J\), and
\(\rho_{s0}\bar v_{s0}=\phi_{s0}\).  Following Foster and Xu
\citep{fosterxu2025}, \(p\) denotes the fluid pore pressure.  The isothermal pressure conjugacy
\(\partial\psi_s/\partial\bar v_s=-p\) then gives the following
mixture-reference relation.  Structural tensors and history variables are
suppressed and held fixed below.  Local pressure equilibrium determines the
mineral volume coordinate:
\begin{equation}
 \left.\pder{W}{q}\right|_{\F}=-\phi_{s0}p\bar J.
 \label{eq:mineral-coordinate-equilibrium}
\end{equation}
Equivalently, the volume derivative at fixed \(\F\) is
\begin{equation}
 \left.\pder{W}{\bar J}\right|_{\F}=-\phi_{s0}p.
 \label{eq:pressure-conjugacy}
\end{equation}
Consider an already-deformed specimen that is rotated rigidly in space, with
its shape and pore pressure left unchanged.  This change of laboratory
orientation cannot affect the stored energy or the mineral volume selected by
local equilibrium.  Thus, \(\bar J\) does not depend on how the specimen is
oriented.

\section{Finite-deformation Biot tensor}
\label{sec:finite-biot}

Assume that \eqref{eq:mineral-coordinate-equilibrium} can be solved locally
for \(q=q(\F,p)\); specifically, assume that the derivative of
\(W_q+\phi_{s0}p\bar J\) with respect to \(q\) is nonzero.  Define the
reduced fixed-pressure energy and its
Legendre transform by
\begin{subequations}
\label{eq:fixed-pressure-energies}
\begin{align}
 W^{\prime\prime}(\F,p)&=W\bigl(\F,q(\F,p)\bigr),
 \label{eq:reduced-energy}\\
 W^{\prime}(\F,p)&=W^{\prime\prime}(\F,p)+\phi_{s0}p\,
 \bar J\bigl(q(\F,p)\bigr).
 \label{eq:legendre-energy}
\end{align}
\end{subequations}
In our earlier work \citep[eq.~(33)]{fosterxu2025}, we transformed the specific
internal energy according to
\(\widetilde e^s(\F,p)=e^s(\F,\bar v_s)+p\bar v_s\).  Equation
\eqref{eq:legendre-energy} is its isothermal Helmholtz counterpart.  Replacing
\(e^s\) by \(\psi_s\), multiplying by \(\rho_{s0}\), and using
\(\bar v_s=\bar v_{s0}\bar J\) together with
\(\rho_{s0}\bar v_{s0}=\phi_{s0}\) gives
\(W+\phi_{s0}p\bar J=\rho_{s0}(\psi_s+p\bar v_s)\).  Evaluating this
identity at the local solution \(q(\F,p)\) gives
\eqref{eq:legendre-energy} per unit reference mixture volume.
The first Piola stresses conjugate to \(\F\), respectively at a fixed mineral
state and at a fixed pore pressure, are
\begin{subequations}
\label{eq:effective-stresses}
\begin{align}
 \bm P^{\prime}&=\left.\pder{W}{\F}\right|_{q}
 =\left.\pder{W^{\prime}}{\F}\right|_p,
 \label{eq:fixed-mineral-state-stress}\\
 \bm P^{\prime\prime}&=\left.\pder{W^{\prime\prime}}{\F}\right|_p.
 \label{eq:fixed-pressure-stress}
\end{align}
\end{subequations}
The second equality in the first definition follows because the change in
\(q\) cancels through
\eqref{eq:mineral-coordinate-equilibrium}.  Differentiating
\eqref{eq:legendre-energy} gives
\begin{equation}
 \bm P^{\prime}=
 \bm P^{\prime\prime}+
 \phi_{s0}p\left.\pder{\bar J}{\F}\right|_p.
 \label{eq:piola-transform-general}
\end{equation}

The total mixture first Piola stress is
\(\bm P=\bm P^{\prime}-pJ\F^{-T}\).  The first term contains the stress
transmitted through the skeleton and mineral state, while the second is the
usual pore-pressure traction.  We define the finite-deformation Biot tensor in
the current configuration by
\begin{equation}
 \bm B
 =\I-\frac{\phi_{s0}}{J}
 \left.\pder{\bar J}{\F}\right|_p\F^T.
 \label{eq:finite-biot-tensor}
\end{equation}
Equations \eqref{eq:piola-transform-general} and \eqref{eq:finite-biot-tensor}
then give the familiar effective-stress form
\begin{equation}
 \bm\sigma=\bm\sigma^{\prime\prime}-p\bm B,
 \label{eq:total-stress-transform}
\end{equation}
where \(\bm\sigma^{\prime\prime}=J^{-1}\bm P^{\prime\prime}\F^T\).
This decomposition does not imply that \(\bm\sigma^{\prime\prime}\) is
independent of pressure. The experimentally accessible pressure tangent
follows from the same potential. Smoothness on a selected equilibrium branch
gives
\begin{align}
 \left.\pder{W'}{p}\right|_{\F}&=\phi_{s0}\bar J,
 \label{eq:pressure-envelope}\\
 \left.\pder{\bm P}{p}\right|_{\F}
 &=\phi_{s0}\left.\pder{\bar J}{\F}\right|_p-J\F^{-T},
 \label{eq:piola-pressure-tangent}\\
 \left.\pder{\bm\sigma}{p}\right|_{\F}&=-\bm B(\F,p).
 \label{eq:cauchy-pressure-tangent}
\end{align}
The second step uses equality of the mixed derivatives of \(W'\).
Equivalently, with pore volume per reference mixture volume
\(\Phi=J-\phi_{s0}\bar J\), the tensor is its pushed-forward deformation
sensitivity:
\begin{equation}
 \bm B=J^{-1}\left.\pder{\Phi}{\F}\right|_p\F^T.
 \label{eq:pore-volume-biot}
\end{equation}
This is the volume-conjugacy interpretation associated with a skeleton
potential in \((\F,\Phi)\) \citep{debuhan1998}. The mineral-volume coordinate
makes the constituent response explicit without creating a new conjugacy.
For a pressure experiment at fixed deformation,
\begin{align}
 \bm\sigma(\F,p)-\bm\sigma(\F,0)
 &=-\int_0^p\bm B(\F,\pi)\,\dd\pi,
 \label{eq:integrated-pressure-response}\\
 \left.\pder{\bm\sigma^{\prime\prime}}{p}\right|_{\F}
 &=p\left.\pder{\bm B}{p}\right|_{\F}.
 \label{eq:effective-stress-pressure-dependence}
\end{align}
Thus using \(-p\bm B(\F,p)\) with the zero-pressure stress generally gives
an incorrect finite-pressure response. In a displacement-controlled test,
\eqref{eq:cauchy-pressure-tangent} instead identifies \(\bm B\) from the
stress changes needed to hold the specimen's shape and volume fixed.

There is also an integrability restriction on any proposed finite-state
tensor. On a coaxial deformation family with logarithmic principal strains
\(\varepsilon_i\), \eqref{eq:pore-volume-biot} implies
\begin{align}
 \left.\pder{\Phi}{\varepsilon_i}\right|_p&=JB_i,
 \label{eq:coaxial-volume-gradient}\\
 \frac{\partial(JB_i)}{\partial\varepsilon_j}
 &=\frac{\partial(JB_j)}{\partial\varepsilon_i}.
 \label{eq:biot-integrability}
\end{align}
All derivatives in the second line hold pressure fixed. If the spatial
principal values \(B_i\) are constants on an open set of these strains,
then \(\partial J/\partial\varepsilon_i=J\) makes
\eqref{eq:biot-integrability} require \(B_i=B_j\). Therefore a spatially
constant nonspherical Biot tensor is not compatible with a smooth
hyperelastic pore-volume potential on that domain. It can be a reference
tangent or a prescribed-path approximation, but a finite-deformation
continuation must include the state dependence needed by this condition.
This is a consequence of mixed-derivative symmetry, not a prohibition on
anisotropic coupling or on rate-based constitutive theories.

Under the rotation-invariance assumption stated above, a superposed rigid
rotation \(\bm Q\) gives \(\bm B(\bm Q\F,p)=\bm Q\bm B(\F,p)\bm Q^T\),
and the same relation holds for \(\bm\sigma^{\prime\prime}\).  For comparison,
suppose that the mineral volume responds only to skeleton volume and not to
skeleton shape.  Then \(\bar J\) depends on \(\F\) only through \(J\), and
\begin{equation}
 \bm B=\left(1-\phi_{s0}\left.\pder{\bar J}{J}\right|_p\right)\I,
 \label{eq:scalar-recovery}
\end{equation}
which is the scalar finite-deformation result derived in our earlier work
\citep[eq.~(39)]{fosterxu2025}.  The normalization used there gives the same
expression exactly: the reference specific volume is \(v_{s0}=1/\rho_{s0}\), where
\(\rho_{s0}=\phi_{s0}\bar\rho_{s0}\), whereas the intrinsic specific volume
is \(\bar v_s=\bar J/\bar\rho_{s0}\).  Hence
\begin{equation}
 \frac{\bar v_s}{v_{s0}}=\phi_{s0}\bar J,
 \qquad
 B=1-\phi_{s0}\left.\pder{\bar J}{J}\right|_p.
 \label{eq:foster-xu-normalization-bridge}
\end{equation}
This is equation~(39) of \citet{fosterxu2025}, expressed using the present
volume ratio.

The tensor in \eqref{eq:finite-biot-tensor} is symmetric under the stated
rotation-invariance assumption.  To show this, take a superposed infinitesimal rotation
\(\bm Q=\I+\bm\Omega\), \(\bm\Omega^T=-\bm\Omega\).  Invariance of
\(\bar J\) gives
\begin{equation}
 \left.\pder{\bar J}{\F}\right|_p:(\bm\Omega\F)=0,
 \qquad\text{so}\qquad
 \left.\pder{\bar J}{\F}\right|_p\F^T
 \ \text{is symmetric}.
 \label{eq:biot-tensor-symmetry}
\end{equation}

\section{Finite-deformation Hencky realization}
\label{sec:finite-hencky}

The kinematic result above does not require a particular elastic law.  We now
give one objective finite-deformation realization that respects the
isotropic-distention split.  Let \(\F=\bm R\bm U\) and
\(\Fb=\bar{\mathbf R}\bar{\mathbf U}\) be right polar decompositions, and
introduce the material logarithmic strains
\begin{equation}
 \bm\varepsilon=\log\bm U,
 \qquad
 \bm\eta=\log\bar{\mathbf U}.
 \label{eq:hencky-strain-coordinates}
\end{equation}
Because \(\A=a\I\), \(\bar{\mathbf U}=a^{-1}\bm U\).  Thus \(\bm\eta\)
is not an independent state variable: it is fixed by \(\bm\varepsilon\) and
\(q\).  Introduce the volumetric and deviatoric projectors on symmetric
tensors,
\begin{equation}
 \Pvol=\frac13\I\otimes\I,
 \qquad
 \Pdev=\mathbb I_{\mathrm{sym}}-\Pvol.
 \label{eq:volumetric-deviatoric-projectors}
\end{equation}
Then
\begin{equation}
 \bm\eta=\Pdev:\bm\varepsilon+\frac{q}{3}\I.
 \label{eq:constrained-mineral-hencky-strain}
\end{equation}
In particular, \(\tr\bm\eta=q=\ln\bar J\) exactly.  Let
\(W_{\mathrm H}(\bm\varepsilon,q)\) be any objective Hencky energy per unit
reference mixture volume, with structural tensors understood in the material
frame.  At prescribed pore pressure, the scalar mineral-volume equilibrium is
\begin{equation}
 \frac{\partial W_{\mathrm H}}{\partial q}
 +\phi_{s0}p\exp(q)=0.
 \label{eq:finite-hencky-mineral-equilibrium}
\end{equation}
Define the scalar fixed-pressure mineral tangent by
\begin{equation}
 h_p=\frac{\partial^2 W_{\mathrm H}}{\partial q^2}
 +\phi_{s0}p\exp(q).
 \label{eq:finite-hencky-mineral-tangent}
\end{equation}
For \(h_p\ne0\), differentiation of
\eqref{eq:finite-hencky-mineral-equilibrium} at fixed pressure gives
\begin{equation}
 \left.\frac{\partial q}{\partial\bm\varepsilon}\right|_p
 =-h_p^{-1}\frac{\partial^2 W_{\mathrm H}}
 {\partial q\,\partial\bm\varepsilon}.
 \label{eq:finite-hencky-mineral-sensitivity}
\end{equation}
Since \(\dd\bar J=\bar J\,\dd q\),
\eqref{eq:finite-hencky-mineral-sensitivity} and the chain rule from
\(\bm\varepsilon(\F)=\log\bm U\) give
\begin{equation}
 \left.\pder{\bar J}{\F}\right|_p
 =-\bar J h_p^{-1}
 \frac{\partial^2 W_{\mathrm H}}
 {\partial q\,\partial\bm\varepsilon}:
 \frac{\partial\bm\varepsilon}{\partial\F}.
 \label{eq:finite-hencky-volume-sensitivity}
\end{equation}
Here \(\partial\bm\varepsilon/\partial\F\) is the fourth-order tangent
that maps \(\dd\F\) to \(\dd\bm\varepsilon\).  Substitution in
\eqref{eq:finite-biot-tensor} yields
\begin{equation}
 \bm B=\I+\frac{\phi_{s0}\bar J}{J}h_p^{-1}
 \left[\frac{\partial^2 W_{\mathrm H}}
 {\partial q\,\partial\bm\varepsilon}:
 \frac{\partial\bm\varepsilon}{\partial\F}\right]\F^T.
 \label{eq:finite-hencky-biot-tensor}
\end{equation}
At the zero-pressure reference state,
\(\bm\varepsilon=\bm\eta=\bm0\), \(J=\bar J=1\),
\(\left.\partial\bm\varepsilon/\partial\F\right|_{\F=\I}
=\mathbb I_{\mathrm{sym}}\), the identity map on symmetric tensors.
Equation~\eqref{eq:finite-hencky-biot-tensor} therefore becomes
\begin{equation}
 \bm B_0=\I+\phi_{s0}h_0^{-1}
 \left.\frac{\partial^2 W_{\mathrm H}}
 {\partial q\,\partial\bm\varepsilon}\right|_0,
 \qquad
 h_0=\left.\frac{\partial^2 W_{\mathrm H}}{\partial q^2}\right|_0.
 \label{eq:finite-hencky-reference-biot-tensor}
\end{equation}
Thus directional pressure coupling arises from a mixed mineral-volume--skeleton
strain tangent, without assigning an independent mineral-shape equilibrium.

\subsection{Admissibility of the equilibrium branch}

The condition \(h_p\ne0\) is an implicit-function condition. A local
minimum with respect to mineral volume at fixed deformation and pressure
requires \(h_p>0\). In addition, a saturated porous state must satisfy
\begin{equation}
 J>0,\qquad 0<\frac{\phi_{s0}\exp(q)}{J}<1.
 \label{eq:porosity-admissibility}
\end{equation}
These restrictions neither imply global existence of the branch nor guarantee
elastic stability with respect to skeleton deformation. That stability must be
checked for the condensed total potential \(W'(\F,p)-pJ\), including the
geometric effects of finite deformation. Convexity in logarithmic strain alone
is not a proof of rank-one convexity in \(\F\).

\section{Generalized-Hooke specialization}
\label{sec:hooke}

For \(\F=\I+\nabla\bm u\), let \(\bm e=\sym\nabla\bm u\).  The
linearization of \eqref{eq:constrained-mineral-hencky-strain} is
\begin{equation}
 \bm\varepsilon=\bm e+O(\lVert\nabla\bm u\rVert^2),
 \qquad
 \bm\eta=\Pdev:\bm e+\frac{q}{3}\I+o(\lVert\nabla\bm u\rVert+|q|).
 \label{eq:hencky-small-strain-limit}
\end{equation}
To expose the mineral-stiffness contribution, begin with the quadratic
Hencky tangent
\begin{equation}
 W_{\mathrm H,0}(\bm e,\bm\eta)=
 \frac12\bm e:\C:\bm e+
 \bm e:\D:\bm\eta+
 \frac12\bm\eta:\Hh:\bm\eta,
 \label{eq:underlying-quadratic-energy}
\end{equation}
Here \(\C\) is a fourth-order skeleton energy coefficient, not the
measured drained stiffness. The fourth-order tensor \(\Hh\) represents
mineral-solid elasticity in
an energy measured per unit reference mixture volume.  The latter
normalization is a volume scaling, not an additional mineral property.  Let
\(\bar W_{m,0}(\bm\eta)\) be the mineral energy per unit reference mineral
volume and let \(\bar{\mathbb H}\) be its fourth-order elasticity tensor.
For a uniform reference
mixture, its contribution to the mixture-reference energy and its tangent are
\begin{equation}
 W_{m,0}=\phi_{s0}\bar W_{m,0},
\qquad
 \Hh=\phi_{s0}\bar{\mathbb H},
\qquad
 \bar{\mathbb H}
 =\left.\frac{\partial^2\bar W_{m,0}}
 {\partial\bm\eta\,\partial\bm\eta}\right|_{\bm\eta=\bm0}.
 \label{eq:mixture-reference-mineral-elasticity}
\end{equation}
Thus \(\bar{\mathbb H}\), rather than \(\Hh\), is the tensor that can
be identified from an isolated mineral specimen, provided that specimen is
represented by the same mineral constitutive state.  The tensor \(\Hh\)
then places that measured response on the reference volume of the porous
mixture.  Both \(\C\) and \(\Hh\) are self-adjoint on symmetric tensors.
The fourth-order tensor \(\D\) maps mineral strain to skeleton stress and
represents their elastic coupling.  The constrained relation in
\eqref{eq:hencky-small-strain-limit} reduces
\eqref{eq:underlying-quadratic-energy} to
\begin{equation}
 W_0(\bm e,q)=
 \frac12\bm e:\C^c:\bm e+
 q\,\bm g:\bm e+
 \frac12 hq^2,
 \label{eq:quadratic-energy}
\end{equation}
where \(\C^c\) is the constrained skeleton tangent, \(\bm g\) is the
mineral-volume--skeleton coupling, and \(h>0\) is the mineral-volume tangent.
Direct substitution gives
\begin{align}
 \C^c&=\C+2\sym(\D:\Pdev)+\Pdev:\Hh:\Pdev,
 \label{eq:constrained-skeleton-tangent}\\
 \bm g&=\frac13\left(\D:\I+\Pdev:\Hh:\I\right),
 \label{eq:reduced-volume-strain-coupling}\\
 h&=\frac19\I:\Hh:\I.
 \label{eq:reduced-mineral-volume-tangent}
\end{align}
Here \(\sym(\D:\Pdev)=\tfrac12(\D:\Pdev+\Pdev:\D^T)\).  Thus,
mineral anisotropy enters \(\bm g\) whenever \(\Hh:\I\) has a deviatoric
part.  Mineral equilibrium under pore pressure is
\begin{equation}
 \bm g:\bm e+hq=-\phi_{s0}p,
 \label{eq:linear-mineral-equilibrium}
\end{equation}
and hence
\begin{equation}
 q=-h^{-1}\bm g:\bm e-h^{-1}\phi_{s0}p.
 \label{eq:mineral-strain-solution}
\end{equation}
At \(p=0\), elimination of \(q\) gives the drained skeleton stiffness,
\begin{equation}
 \C^d=\C^c-h^{-1}\bm g\otimes\bm g.
 \label{eq:drained-stiffness}
\end{equation}
Here \((\bm g\otimes\bm g):\bm e=\bm g(\bm g:\bm e)\).
Since \(\bar J=1+q+o(|q|)\), \eqref{eq:mineral-strain-solution}
and \eqref{eq:finite-biot-tensor} yield the small-strain Biot tensor
\begin{equation}
 \boxed{\quad
 \bm B_0=\I+\phi_{s0}h^{-1}\bm g.
 \quad}
 \label{eq:linear-biot-tensor}
\end{equation}
The stress relation is \(\bm\sigma=\bm\sigma^{\prime\prime}-p\bm B_0\).
Within the freely specified reduced energy, \(\C^d\) and \(h\) alone do
not determine \(\bm g\). For any chosen \(\bm g\), setting
\(\C^c=\C^d+h^{-1}\bm g\otimes\bm g\) preserves the drained tangent.
This freedom is restricted if additional constituent compatibility is imposed,
as shown below. Stability of this reduced quadratic energy requires both
\(h>0\) and positive definiteness of \(\C^d\). The daughter tensors are
an energy decomposition on the constrained state space; positivity of their
individual diagonal blocks does not by itself prove stability of the coupled
energy.

\section{Special cases}
\label{sec:special-cases}

\subsection{Isotropic mineral stiffness and anisotropic skeleton coupling}

Let the mineral stiffness be isotropic,
\begin{equation}
 \Hh=\phi_{s0}\left[
 \lambda_s\,\I\otimes\I+2\mu_s\,\mathbb I_{\mathrm{sym}}
 \right],
 \qquad K_s=\lambda_s+\frac23\mu_s.
 \label{eq:isotropic-mineral-stiffness}
\end{equation}
Then \(\Pdev:\Hh:\I=\bm0\) and \(h=\phi_{s0}K_s\), so
\begin{equation}
 \bm g=\frac13\D:\I,
 \qquad
 \bm B_0=\I+\frac{1}{3K_s}\D:\I.
 \label{eq:isotropic-mineral-biot-tensor}
\end{equation}
The constrained and drained skeleton tangents may be anisotropic.  With an
isotropic mineral, directional pressure coupling is carried by the volumetric
part \(\D:\I\) of the skeleton--mineral coupling.

\subsection{Anisotropic mineral stiffness with isotropic coupling}

Let the coupling be isotropic,
\begin{equation}
 \D=d_v\Pvol+d_d\Pdev.
 \label{eq:isotropic-skeleton-mineral-coupling}
\end{equation}
For an arbitrary positive-definite mineral stiffness \(\Hh\), equations
\eqref{eq:reduced-volume-strain-coupling}--\eqref{eq:linear-biot-tensor}
give
\begin{equation}
 \bm B_0=
 \I+\frac{\phi_{s0}}{3h}
 \left(d_v\I+\Pdev:\Hh:\I\right),
 \qquad h=\frac19\I:\Hh:\I.
 \label{eq:anisotropic-mineral-biot-tensor}
\end{equation}
Consequently, an anisotropic mineral stiffness can produce a directional Biot
tensor even when the skeleton--mineral coupling is isotropic.  The directional
part is proportional to \(\Pdev:\Hh:\I\), which measures
volumetric--deviatoric coupling in the mineral stiffness.

\subsection{Mineral anisotropy without volumetric--deviatoric coupling}

If the anisotropic mineral stiffness satisfies
\begin{equation}
 \Pdev:\Hh:\I=\bm0,
 \label{eq:mineral-volumetric-deviatoric-decoupling}
\end{equation}
then the mineral may remain anisotropic in its deviatoric block
\(\Pdev:\Hh:\Pdev\), but it does not produce directional pressure coupling
through \eqref{eq:anisotropic-mineral-biot-tensor}.  For the isotropic
coupling \eqref{eq:isotropic-skeleton-mineral-coupling},
\begin{equation}
 \bm B_0=\left(1+\frac{\phi_{s0}d_v}{3h}\right)\I.
 \label{eq:decoupled-mineral-biot-tensor}
\end{equation}
Changing only the deviatoric block while holding \(h\) and \(\D\) fixed
leaves the entire Biot tensor unchanged. Changing the volumetric block can
change its spherical part.

\subsection{Volumetric calibration for the classical coefficient}
\label{sec:volumetric-calibration}

The quadratic family includes a volumetric calibration that recovers the
classical scalar Biot coefficient.  Take the isotropic mineral stiffness
\eqref{eq:isotropic-mineral-stiffness} and set
\begin{equation}
\D=-3K\Pvol+d_d\Pdev,
\qquad
\C^c=\C^d+\frac{K^2}{\phi_{s0}K_s}\I\otimes\I.
 \label{eq:volumetric-energy}
\end{equation}
Here \(K\) and \(K_s\) are the drained skeleton and mineral bulk moduli.
Equations \eqref{eq:reduced-volume-strain-coupling} and
\eqref{eq:reduced-mineral-volume-tangent} give \(\bm g=-K\I\) and
\(h=\phi_{s0}K_s\).  The Schur complement in
\eqref{eq:drained-stiffness} then returns \(\C^d\), and
\eqref{eq:linear-biot-tensor} gives
\begin{equation}
 \bm B_0=\left(1-\frac{K}{K_s}\right)\I,
 \qquad B=1-\frac{K}{K_s}.
 \label{eq:classical-biot}
\end{equation}
The deviatoric coefficient \(d_d\) does not enter \(\bm B_0\), but it
contributes to the constrained tangent in
\eqref{eq:constrained-skeleton-tangent}.
Positive bulk and shear moduli are required for isotropic elastic response.  If
the drained skeleton is no stiffer in bulk than its unjacketed mineral
response, \(0\leq K\leq K_s\), then \eqref{eq:classical-biot} gives
\(0\leq B\leq1\) \citep{makhnenkolabuz2016}.  This modulus ordering is a
material restriction and is not a consequence of tensor anisotropy alone.

\section{Compatibility with homogeneous-solid unjacketed loading}
\label{sec:compatibility}

An unjacketed pressure increment applies the same compressive pressure to the
external boundary and the pore surfaces. For a homogeneous elastic solid
constituent, the classical uniform-stress solution provides an additional
constitutive constraint. Let \(\bar{\mathbb S}=\bar{\mathbb H}^{-1}\), and
define the intrinsic hydrostatic compressibility tensor
\(\bm a=\bar{\mathbb S}:\I\). The target reference response is
\begin{equation}
 \bm\sigma=-p\I,\qquad
 \bm e=-p\bm a,\qquad q=-p\tr\bm a.
 \label{eq:unjacketed-target}
\end{equation}
Here the mineral and specimen undergo the same uniform strain. This is a
specific microscopic compatibility assumption, not a consequence of local
pressure equilibrium alone. It is consistent with the isotropic-distention
constraint because the target distention increment is zero.
Substitution into the reference stress relation gives
\begin{align}
 \bm B_0&=\I-\C^d:\bm a,
 \label{eq:classical-anisotropic-compatibility}\\
 \bm g&=-\frac{h}{\phi_{s0}}\C^d:\bm a.
 \label{eq:mixed-tangent-compatibility}
\end{align}
For isotropic intrinsic elasticity, \(\bm a=\I/(3K_s)\); the first
expression is the microisotropic modulus-based tensor used in classical
anisotropic poroelasticity \citep[sec.~2.1]{wong2017} and in the elastic
coupling of Zhao and Borja \citep{zhaoborja2020}. The second expression
specifies the previously free mixed tangent once \(h\) is known.

Recovering the mineral-volume part of \eqref{eq:unjacketed-target} imposes
another condition. Applying the same loading to
\eqref{eq:linear-mineral-equilibrium} gives
\begin{align}
 \bm g:\bm a+h\tr\bm a&=\phi_{s0},
 \label{eq:unjacketed-volume-compatibility}\\
 h&=\frac{\phi_{s0}^2}
 {\phi_{s0}\tr\bm a-\bm a:\C^d:\bm a},
 \qquad
 \phi_{s0}\tr\bm a>\bm a:\C^d:\bm a.
 \label{eq:compatible-mineral-tangent}
\end{align}
The inequality is necessary for a stable positive mineral-volume tangent in
this reduced model. These equations are a constitutive compatibility test:
recovering the stress coefficient does not alone establish that the mineral
volume or storage response has the classical limit.

In the fully isotropic case, the two compatibility conditions reduce to
\begin{equation}
 h_{\mathrm{uj}}=\frac{\phi_{s0}^2K_s^2}{\phi_{s0}K_s-K},
 \qquad
 \bm g_{\mathrm{uj}}=-\frac{h_{\mathrm{uj}}K}{\phi_{s0}K_s}\I,
 \qquad K<\phi_{s0}K_s.
 \label{eq:compatible-isotropic-tangents}
\end{equation}
The stress-only calibration in \cref{sec:volumetric-calibration}, by contrast,
sets \(h=\phi_{s0}K_s\) and \(\bm g=-K\I\). On the same unjacketed
path its mineral equilibrium predicts
\begin{equation}
 q=-\frac{p}{K_s}\left(1+\frac{K}{\phi_{s0}K_s}\right),
 \label{eq:stress-only-unjacketed-mismatch}
\end{equation}
rather than \(-p/K_s\) for \(K>0\). The factor multiplying \(p/K_s\)
is the consequence of using the isolated-mineral tangent as the fixed-skeleton
mineral-volume tangent. These are different experiments.

Consequently, the restricted daughter energy with
\(\Hh=\phi_{s0}\bar{\mathbb H}\) cannot, in general, reproduce both
classical conditions. One possible extension adds an independent pore-constraint
energy \(\tfrac12\kappa q^2\) to
\eqref{eq:underlying-quadratic-energy}. In that extended family,
\begin{equation}
 h=\frac19\I:\Hh:\I+\kappa,
 \qquad
 \kappa=h_{\mathrm{uj}}-\phi_{s0}K_s
 \quad\text{in the isotropic case}.
 \label{eq:additional-volume-energy}
\end{equation}
The mixed coupling and constrained skeleton tangent must also be selected to
satisfy \eqref{eq:mixed-tangent-compatibility} and
\eqref{eq:drained-stiffness}. This extension assigns the measured intrinsic
elasticity to \(\Hh\) while allowing the cost of changing mineral volume
at fixed skeleton deformation to include pore constraints. It does not change
the direct mineral contribution to \(\bm g\) in
\eqref{eq:reduced-volume-strain-coupling}. The examples below use the
compatible reduced energy, with \(h\) an effective tangent rather than an
isolated-mineral modulus.

\subsection{Identification and storage}

At the reference state, drained tests identify \(\C^d\); equilibrium
pressure perturbations at fixed deformation identify \(\bm B_0\).
Intrinsic-density measurements at fixed skeleton strain provide
\begin{equation}
 \left.\pder{q}{p}\right|_{\bm e}=-\frac{\phi_{s0}}{h},
 \qquad
 \bm g=\frac{h}{\phi_{s0}}(\bm B_0-\I).
 \label{eq:reference-identification}
\end{equation}
The specimen must be allowed to equilibrate with the pressure reservoir;
fixed deformation is not the same condition as zero fluid exchange. If
pressure loading is performed at constant total stress instead, the measured
strain sensitivity must be combined with \(\C^d\) to infer \(\bm B_0\).
The range of laboratory loadings used by Braun et al.\ \citep{braun2020}
illustrates this distinction. Recovering \(\bm g\) from measured coupling
is identification, not an independent validation of the model.

For the reference pore-volume increment and the increment of fluid content
\(\zeta\), measured as fluid mass divided by reference intrinsic fluid
density and reference mixture volume, the linearized mass relation is
\begin{align}
 \delta\Phi&=\bm B_0:\bm e+\frac{\phi_{s0}^2}{h}p,
 \label{eq:reference-pore-volume}\\
 \zeta&=\bm B_0:\bm e+
 \left(\frac{\phi_{s0}^2}{h}+\frac{\phi_{f0}}{K_f}\right)p.
 \label{eq:reference-fluid-content}
\end{align}
Here \(\phi_{f0}=1-\phi_{s0}\), and \(K_f>0\) is the fluid bulk modulus.
These expressions follow from solid mass conservation and a linearized fluid
equation of state. The solid-storage coefficient \(S_s=\phi_{s0}^2/h\)
under homogeneous-solid compatibility can be written
\begin{align}
 S_s&=\phi_{s0}\tr\bm a-\bm a:\C^d:\bm a
      =\bm a:(\bm B_0-\phi_{f0}\I),
 \label{eq:classical-solid-storage}\\
 S_s&=\frac{\tr\bm B_0/3-\phi_{f0}}{K_s}
 \quad\text{for an isotropic intrinsic solid}.
 \label{eq:microisotropic-solid-storage}
\end{align}
The latter is the established solid-storage term in Zhao and Borja
\citep[eqs.~(17), (29), (42)]{zhaoborja2020}; it becomes
\((B-\phi_{f0})/K_s\) in the fully isotropic case. Thus
\eqref{eq:compatible-mineral-tangent} is classical storage compatibility
expressed in the \(q\) coordinate. The extra coefficient \(\kappa\) in
\eqref{eq:additional-volume-energy} restores that existing constitutive
freedom within a restricted energy decomposition; it is not a newly
independent physical coefficient. The useful diagnosis is that assigning
\(h=\phi_{s0}K_s\) instead gives \(S_s=\phi_{s0}/K_s\) despite a correct
stress coefficient. A storage measurement distinguishes those assignments.
At finite deformation the same construction gives
\(\Phi_p=\phi_{s0}^2\bar J^2/h_p>0\) on the stable branch.

\input{sections/experiments}

\section{Discussion and conclusions}
\label{sec:conclusions}

The finite-deformation tensor is a pressure tangent and a pushed-forward
pore-volume sensitivity. Its effective-stress decomposition follows from
mineral-volume equilibrium, but the pressure-independent stress used in a
finite pressure increment must be distinguished from
\(\bm\sigma^{\prime\prime}\). The integral pressure response makes that
distinction explicit and provides a direct test of a constitutive
implementation.

The constrained quadratic reduction identifies the direct mineral contribution
through \(\Pdev:\Hh:\I\). This statement requires isotropic distention
and the stated energy decomposition. It is not a general theorem that all
mineral anisotropy affects pressure coupling in this way. Even in that family,
\(\bm B_0\) and \(\Hh\) determine only
\(\D:\I=3\bm g-\Pdev:\Hh:\I\); they do not identify the full coupling
tensor. Changes to \(\D\) can compensate for changes in the mineral
contribution, so independent constituent information is required to attribute
measured directionality to a mechanism.

Classical unjacketed stress and storage compatibility provide a restrictive
test of the reference calibration. A model can reproduce \(1-K/K_s\) in stress
and still predict the wrong mineral volume and storage. Enforcing both
responses fixes the effective mineral-volume tangent and explains why a
volume-scaled isolated-mineral modulus is generally insufficient. The added
pore-constraint energy restores the established independent storage freedom
to that ansatz; it is not a unique microscopic construction. When compatibility
is enforced, the mineral compliance and the recalibrated coupling act together.
A direct stiffness-block attribution made at fixed coupling must not be
transferred unchanged to this constrained calibration.

The pressure and confined-layer examples show how finite-state coupling can
alter measurable loads and displacements even when the reference anisotropic
tensor and the entire zero-pressure response are reproduced by a simpler
approximation. The volume-penalty continuation demonstrates that those reference data
do not uniquely select the finite-pressure response. Pressure curvature at
fixed deformation distinguishes the two energies, while the integrability
condition rules out a globally hyperelastic interpretation of a spatially
frozen nonspherical tensor. These are constitutive tests, not experimental
validation or universal error thresholds. Small values of
\(p/K_s\) and small strains reduce these differences; for stiff minerals,
large elastic strains may be precluded by damage or plasticity before the
illustrative regime is reached. Application to a rock therefore requires
independent calibration over its reversible range, verification of branch
admissibility, and a comparison with measured pressure and deformation paths.
Anisotropic distention, evolving pore fabric, and inelasticity would require
additional constitutive variables beyond the present elastic model.

\section*{Acknowledgements}

The author thanks Professor D. Nicolas Espinoza. This work was funded by the
U.S. Department of Energy under awards DE-FE0032349, ``Hydrogen Storage in Salt
Caverns in the Permian Basin: Seal Integrity Evaluation and Field Test,'' and
DE-AR0001864283, assigned control number 2784-1755, ``Cyclic Injection for
Commercial Seismic-Safe Geologic H$_2$ Production.''

\input{provenance/ai_use_statement.tex}

\bibliographystyle{plainnat}
\bibliography{references}
\end{document}
